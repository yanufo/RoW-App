name: filesystem
version: 0.0.1
schema: v1
mcpServers:
  - name: filesystem
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem"
      - "/home/gve/Downloads/row_inspection_renew" # ⚠️ REPLACE THIS with the absolute path to your project folder
